/*Implementá un programa en C para simular la siguiente 
variante del juego del Tatetí.El programa deberá recibir 
como argumentos los nombres de los dos jugadores
(el primer nombre será el del jugador de las cruces
y el segundo el de aquel de los círculos), y luego les irá
solicitando una a una las posiciones del tablero donde
quieran ubicar su marca en cada turno. El programa deberá
terminar al formarse Tatetí o al llegar a un empate, y al
finalizar deberá indicar el resultado de la partida 
(quién ganó o declarando el empate, según corresponda). 
A diferencia del Tatetí tradicional, esta variante tendrá 
un tablero de 5x5, y por ende se deberán formar 5 en línea 
para ganar.

El programa deberá gestionar adecuadamente la memoria que
use, y deberá chequear que no se intenten realizar accesos 
incorrectos a posiciones de memoria que no sean utilizables.

Background: El Tatetí o juego de tres en línea, en su 
versión tradicional, es un juego para dos jugadores que consiste 
de un tablero de 3x3 casilleros en el cual cada jugador (uno 
representado por las cruces y otro por los círculos), alternadamente 
ponen su marca en uno de los casilleros intentando lograr formar una 
línea recta de 3 casilleros con su marca (es decir, 3 cruces o 3
círculos contiguos en linea recta, ya sea horizontal o verticalmente, 
o en diagonal ascendente o descendente). El juego se termina cuando 
alguno de los dos jugadores logra formar Tatetí (tres en línea), o 
cuando se llega a un empate (esto es que ninguno de los 2 jugadores
tenga chances de formar Tatetí).

Entregá todo código fuente que hayas generado que sea necesario 
para su correcta compilación y ejecución (archivos .c y .h únicamente),
 y en el recuadro de texto ingresá el comando utilizado para compilar 
 el programa. Por favor, no incluyas ningún binario. */

#include <stdlib.h>
#include "functionsTateti.c"


int main(int argc, char const *argv[])
{

	/*f (argc == 3){


		printf("EL jugador 1 debe ingresar una posicion");
		scanf("%d",pos1);
		printf("EL jugador 1 debe ingresar otra posicion");
		scanf("%d",pos2);








	}

	else{

		printf("Deben ingresarse 2 jugadores");

	}*/


 loop(t);

}