#include <stdio.h>

int main (int argc, char *argv[]){


	if (argv[1] = "abracadabra") // si coloco "==", en vez de "=", que es lo que compara? preguntar!
		printf("Un juego de rol masivo en linea, es aquel el que atrapa a sus jugadores.. -Dark Souls\n");
	else
		printf("la cantidad de argumentos ingresada es: %d\n", argc-1 );



}